{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-empty-object-type": "off", // Disable the no-empty-object-type rule
    "@typescript-eslint/no-unused-vars": "off" // Disable the unused vars rule
    // Alternatively, you can set it to "warn" if you want to see warnings instead of errors
    // "@typescript-eslint/no-unused-vars": "warn",
  }
}
